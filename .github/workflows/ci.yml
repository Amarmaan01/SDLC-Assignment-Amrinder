name: SDLC Assignment CI

on:
  push:
    branches: [ main ]

jobs:
  check-files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Verify Assignment Files
        run: |
          echo "Checking assignment files..."
          ls
